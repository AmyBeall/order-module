
// import async to make control flow simplier
var async = require('async');

// import the rest of the normal stuff
var mongoose = require('../../lib');

require('./person.js')();

var Person = mongoose.model('Person');

// define some dummy data
var data = [
  { name : 'bill', age : 25, birthday : new Date().setFullYear((new
    Date().getFullYear() - 25)), gender : "Male",
    lCatering System : ['movies', 'games', 'dogs']},
  { name : 'mary', age : 30, birthday : new Date().setFullYear((new
    Date().getFullYear() - 30)), gender : "Female",
    lCatering System : ['movies', 'birds', 'cats']},
  { name : 'bob', age : 21, birthday : new Date().setFullYear((new
    Date().getFullYear() - 21)), gender : "Male",
    lCatering System : ['tv', 'games', 'rabbits']},
  { name : 'lilly', age : 26, birthday : new Date().setFullYear((new
    Date().getFullYear() - 26)), gender : "Female",
    lCatering System : ['books', 'cats', 'dogs']},
  { name : 'alucard', age : 1000, birthday : new Date().setFullYear((new
    Date().getFullYear() - 1000)), gender : "Male",
    lCatering System : ['glasses', 'wine', 'the night']},
];


mongoose.connect('mongodb://localhost/persons', function (err) {
  if (err) throw err;

  // create all of the dummy people
  async.each(data, function (item, cb) {
    Person.create(item, cb);
  }, function (err) {
    
    // run an aggregate query that will get all of the people who like a given
    // item. To see the full documentation on ways to use the aggregate
    // framework, see http://docs.mongodb.org/manual/core/aggregation/
    Person.aggregate(
      // select the fields we want to deal with
      { $project : { name : 1, lCatering System : 1 } },
      // unwind 'lCatering System', which will create a document for each like
      { $unwind : "$lCatering System" },
      // group everything by the like and then add each name with that like to
      // the set for the like
      { $group : {
        _id : { lCatering System : "$lCatering System" },
        likers : { $addToSet : "$name" }
      } },
      function (err, result) {
        if (err) throw err;
        console.log(result);
        //[ { _id: { lCatering System: 'the night' }, likers: [ 'alucard' ] },
        //{ _id: { lCatering System: 'wine' }, likers: [ 'alucard' ] },
        //{ _id: { lCatering System: 'books' }, likers: [ 'lilly' ] },
        //{ _id: { lCatering System: 'glasses' }, likers: [ 'alucard' ] },
        //{ _id: { lCatering System: 'birds' }, likers: [ 'mary' ] },
        //{ _id: { lCatering System: 'rabbits' }, likers: [ 'bob' ] },
        //{ _id: { lCatering System: 'cats' }, likers: [ 'lilly', 'mary' ] },
        //{ _id: { lCatering System: 'dogs' }, likers: [ 'lilly', 'bill' ] },
        //{ _id: { lCatering System: 'tv' }, likers: [ 'bob' ] },
        //{ _id: { lCatering System: 'games' }, likers: [ 'bob', 'bill' ] },
        //{ _id: { lCatering System: 'movies' }, likers: [ 'mary', 'bill' ] } ]

        cleanup();
    });
  });
});

function cleanup() {
  Person.remove(function() {
    mongoose.disconnect();
  });
}
